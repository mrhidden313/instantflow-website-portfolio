"// outcrowd.js — InstantFlow Epic Physics Engine v2.0\n// Physics Boulder Drop · 3D Parallax · Professional Download Experience\n// Desktop ONLY (window.innerWidth > 768)\n\ndocument.addEventListener(\"DOMContentLoaded\", () => {\n    if (window.innerWidth <= 768) return;\n\n    // ═══════════════════════════════════════════════════════════════\n    // 1. LENIS — Butter-smooth scroll\n    // ═══════════════════════════════════════════════════════════════\n    const lenis = new Lenis({\n        duration: 1.4,\n        easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),\n        direction: 'vertical',\n        smooth: true,\n        mouseMultiplier: 1,\n        smoothTouch: false,\n        infinite: false,\n    });\n\n    function raf(time) {\n        lenis.raf(time);\n        requestAnimationFrame(raf);\n    }\n    requestAnimationFrame(raf);\n\n    gsap.registerPlugin(ScrollTrigger);\n    lenis.on('scroll', ScrollTrigger.update);\n    gsap.ticker.add((time) => { lenis.raf(time * 1000); });\n    gsap.ticker.lagSmoothing(0, 0);\n\n    // ═══════════════════════════════════════════════════════════════\n    // 2. 3D SCROLL PARALLAX — Objects move as you scroll\n    // ═══════════════════════════════════════════════════════════════\n    const parallaxTargets = [\n        { selector: '.orb1', yFactor: -0.08, xFactor: 0.03 },\n        { selector: '.orb2', yFactor: 0.06, xFactor: -0.04 },\n        { selector: '.hero-badge', yFactor: -0.04, scale: true },\n        { selector: 'h1', yFactor: -0.03 },\n        { se
<truncated 26500 bytes>